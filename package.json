{
  "name": "flow",
  "version": "1.0.0",
  "main": "packages/app/index.js",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "10.15.0",
    "npm": "6.6.0"
  },
  "scripts": {
    "client": "yarn workspace flow-client start",
    "server": "yarn workspace flow-server dev",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
